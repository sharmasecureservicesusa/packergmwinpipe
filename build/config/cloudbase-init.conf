[DEFAULT]
# ----------------------------------------------------------------------
# Local User & Security Setup
# ----------------------------------------------------------------------
username=Administrator
groups=Administrators
inject_user_password=true
# 'no' prevents Windows from forcing a manual password reset on first RDP logon
first_logon_behaviour=no

# ----------------------------------------------------------------------
# Metadata Services (Ordered by priority)
# ----------------------------------------------------------------------
# ConfigDriveService: Attached virtual CD-ROM/drive (Proxmox, Harvester, OpenStack)
# HttpService: 169.254.169.254 OpenStack / IaaS metadata endpoint
# EC2Service: AWS / OpenStack EC2 metadata compatibility
metadata_services=cloudbaseinit.metadata.services.configdrive.ConfigDriveService,cloudbaseinit.metadata.services.httpservice.HttpService,cloudbaseinit.metadata.services.ec2service.EC2Service

# Config Drive device scan types
config_drive_raw_hhd=true
config_drive_cdrom=true
config_drive_vfat=true

# ----------------------------------------------------------------------
# Plugin Pipeline (Executed in exact order)
# ----------------------------------------------------------------------
plugins=cloudbaseinit.plugins.common.mtu.MTUPlugin,cloudbaseinit.plugins.common.sethostname.SetHostNamePlugin,cloudbaseinit.plugins.windows.extendvolumes.ExtendVolumesPlugin,cloudbaseinit.plugins.windows.createuser.CreateUserPlugin,cloudbaseinit.plugins.windows.setuserpassword.SetUserPasswordPlugin,cloudbaseinit.plugins.common.sshpublickeys.SetUserSSHPublicKeysPlugin,cloudbaseinit.plugins.common.localscripts.LocalScriptsPlugin,cloudbaseinit.plugins.common.userdata.UserDataPlugin

# ----------------------------------------------------------------------
# Execution & Lifecycle Behavior
# ----------------------------------------------------------------------
# Allow hostnames longer than 15 characters (bypasses NetBIOS limit)
netbios_host_name_compatibility=false

# Prevent plugins from rebooting abruptly (let user-data control reboots)
allow_reboot=false
stop_service_on_exit=false
check_latest_version=false

# Retry settings for metadata discovery during slow network initialization
retry_count=5
retry_count_interval=4

# ----------------------------------------------------------------------
# Logging Settings
# ----------------------------------------------------------------------
verbose=true
debug=false
log_dir=C:\Program Files\Cloudbase Solutions\Cloudbase-Init\log\
log_file=cloudbase-init.log
default_log_levels=comtypes=INFO,suds=INFO,iso8601=WARN,requests=WARN
logging_serial_port_settings=COM1,115200,N,8
